const initState = {
    firstNumber: 0,
    secondNumber: 0,
    math: "",
    listResult: []
}

export default function reducer(state = initState, action){
    switch(action){
        default:
            return state;
    }
}